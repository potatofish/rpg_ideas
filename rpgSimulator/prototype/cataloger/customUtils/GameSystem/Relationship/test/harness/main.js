//*jshint node: true, esversion: 9*/
"use strict";

const Relationship = require('Relationship');

const relationshipConfig = {};


async function harness(config) {
    let relationship = new Relationship(config);
};

harness(relationshipConfig);