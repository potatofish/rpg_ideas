//*jshint node: true, esversion: 9*/
"use strict";

const Phase = require('Phase');
const phaseConfig = {};

async function harness(config) {
    let phase = new Phase(config);
};

harness(phaseConfig);